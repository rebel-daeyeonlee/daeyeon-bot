# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
build/
dist/
.eggs/

# uv / venv
.venv/
venv/
.python-version.local

# tooling caches
.ruff_cache/
.pytest_cache/
.pyright/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project
.env
.env.*
!.env.example
config.toml
config.local.toml
*.bak
*.db
*.db-journal
*.db-wal
*.db-shm

# State directory in the source tree (we should never write to it; this is defense in depth)
.daeyeon-bot/

# Logs
*.log
logs/

# Tool-local state (Claude Code, spec runner) — but bundled persona skills
# under `.claude/skills/` are part of the repo and need to be committable.
.claude/*
!.claude/skills/
.specify/

# Bot-managed ssw-bundle clone (auto-created on first triage). See
# specs/002-jira-triage-bot/contracts/ssw-bundle-checkout-surface.md.
var/
